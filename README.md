# Material Admin Panel

Веб-админка на Next.js + shadcn/ui для автоматической загрузки материалов из API каждые 5 минут.

## 🚀 Возможности

- ✅ Автоматическая синхронизация с API каждые 5 минут
- ✅ Современный UI на базе shadcn/ui
- ✅ PostgreSQL база данных
- ✅ Статусы материалов (новый, обработан, архив)
- ✅ Фильтрация и поиск
- ✅ Готово к деплою на Railway
- ✅ Поддержка RSS и Telegram источников

## 📋 Требования

- Node.js 18+
- PostgreSQL база данных
- Доступ к API (https://organic-kangaroo.pikapod.net)

## 🛠 Установка

### 1. Клонируйте репозиторий

```bash
git clone <your-repo>
cd material-admin
```

### 2. Установите зависимости

```bash
npm install
```

### 3. Настройте переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните переменные в `.env`:

```env
API_BASE_URL=https://organic-kangaroo.pikapod.net
API_KEY=your_api_key_here
DATABASE_URL=postgresql://user:password@host:port/database
NODE_ENV=development
```

### 4. Настройте API интеграцию

**ВАЖНО!** Вам нужно настроить подключение к вашему API:

1. Откройте файл `lib/api-service.ts`
2. Найдите функцию `fetchNewMaterials()` и замените эндпоинт на реальный:

```typescript
// Замените это:
const url = `${this.baseUrl}/api/materials`

// На реальный эндпоинт из вашей API документации, например:
const url = `${this.baseUrl}/api/v1/posts`
// или
const url = `${this.baseUrl}/content/recent`
```

3. Настройте функцию `transformApiResponse()` под структуру данных вашего API
4. Если API требует особую аутентификацию, измените заголовки в `fetchWithAuth()`

### 5. Запустите локально

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 🚂 Деплой на Railway

### Вариант 1: Через GitHub (рекомендуется)

1. **Создайте репозиторий на GitHub:**
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin <your-github-repo-url>
git push -u origin main
```

2. **Зайдите на Railway:**
   - Перейдите на [railway.app](https://railway.app)
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Добавьте PostgreSQL:**
   - В вашем проекте нажмите "+ New"
   - Выберите "Database" → "PostgreSQL"
   - Railway автоматически создаст переменную `DATABASE_URL`

4. **Настройте переменные окружения:**
   - Перейдите в Settings → Variables
   - Добавьте:
     - `API_BASE_URL` = `https://organic-kangaroo.pikapod.net`
     - `API_KEY` = ваш API ключ
     - `NODE_ENV` = `production`
   - `DATABASE_URL` уже будет создана автоматически

5. **Деплой:**
   - Railway автоматически задеплоит приложение
   - Получите публичный URL в Settings → Domains

### Вариант 2: Через Railway CLI

```bash
# Установите Railway CLI
npm install -g @railway/cli

# Войдите
railway login

# Инициализируйте проект
railway init

# Добавьте PostgreSQL
railway add --database postgresql

# Установите переменные
railway variables set API_BASE_URL=https://organic-kangaroo.pikapod.net
railway variables set API_KEY=your_api_key
railway variables set NODE_ENV=production

# Деплой
railway up
```

## 📊 Использование

### Автоматическая синхронизация

Приложение автоматически загружает новые материалы каждые 5 минут.

### Ручная синхронизация

Нажмите кнопку "Синхронизировать" в правом верхнем углу.

### Telegram каналы

1. Получите `TELEGRAM_API_ID` и `TELEGRAM_API_HASH` на [my.telegram.org](https://my.telegram.org)
2. Сгенерируйте session string: `npm run telegram:auth`
3. Внесите значения в `/admin/settings` → «Интеграция Telegram»
4. Добавьте публичные каналы в разделе `/admin/telegram`

Подробнее: `docs/TELEGRAM_SETUP.md`.

### Управление статусами

- **Новый** → нажмите "Обработать"
- **Обработан** → нажмите "В архив"
- **Архив** → нажмите "Восстановить"

### Фильтрация

Нажмите на карточки статистики или используйте кнопки фильтров.

## 🔧 Настройка интервала синхронизации

По умолчанию синхронизация происходит каждые 5 минут. Чтобы изменить:

Откройте `lib/cron.ts` и измените cron выражение:

```typescript
// Каждые 5 минут (по умолчанию)
cron.schedule('*/5 * * * *', ...)

// Каждые 10 минут
cron.schedule('*/10 * * * *', ...)

// Каждый час
cron.schedule('0 * * * *', ...)

// Каждый день в 9:00
cron.schedule('0 9 * * *', ...)
```

## 🐛 Устранение проблем

### API не возвращает данные

1. Проверьте логи: `railway logs` или в Railway Dashboard
2. Убедитесь что эндпоинт в `lib/api-service.ts` правильный
3. Проверьте что API ключ корректный
4. Используйте нажмите кнопку "Синхронизировать" для ручной проверки

### База данных не подключается

1. Проверьте что переменная `DATABASE_URL` установлена
2. В Railway убедитесь что PostgreSQL сервис запущен
3. Проверьте логи подключения

### Cron не работает

Railway может "усыплять" бесплатные приложения. Для надежной работы cron:
- Используйте Railway Cron Jobs (платная функция)
- Или настройте внешний cron (например, через cron-job.org) который будет вызывать `/api/sync`

## 📝 Структура проекта

```
material-admin/
├── app/
│   ├── api/
│   │   ├── materials/route.ts  # API для материалов
│   │   ├── stats/route.ts      # API для статистики
│   │   └── sync/route.ts       # API для синхронизации
│   ├── globals.css             # Глобальные стили
│   ├── layout.tsx              # Layout с инициализацией cron
│   └── page.tsx                # Главная страница админки
├── components/ui/              # shadcn/ui компоненты
├── lib/
│   ├── api-service.ts          # Сервис для работы с API
│   ├── db.ts                   # Работа с базой данных
│   ├── cron.ts                 # Cron job
│   ├── types.ts                # TypeScript типы
│   └── utils.ts                # Утилиты
└── package.json
```

## 🔐 Безопасность

- Никогда не коммитьте `.env` файл
- Храните API ключи в переменных окружения
- На продакшене используйте SSL подключение к БД

## 📄 Лицензия

MIT

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи приложения
2. Убедитесь что все переменные окружения установлены
3. Проверьте документацию API
