# Шпаргалка - Syndicate Admin

**Быстрая справка для работы с проектом**

---

## 🔥 Критические проблемы (исправить в первую очередь)

```
┌─────────────────────────────────────────────────────────┐
│ P0 - БЛОКЕРЫ (нельзя добавлять функционал без этого)   │
├─────────────────────────────────────────────────────────┤
│ 1. Нет архитектурных слоев      → 2 дня рефакторинга   │
│ 2. Cron в React layout          → 0.5 дня исправления  │
│ 3. Нет обработки ошибок         → 0.5 дня исправления  │
│ 4. Нет типизации API            → 1 день исправления   │
└─────────────────────────────────────────────────────────┘
```

---

## 🚀 Сценарии работы

### Сценарий А: "Есть время на рефакторинг" (рекомендуется)
```bash
# Шаг 1: Создать ветку
git checkout -b refactor/architecture-improvements

# Шаг 2: Установить зависимости
npm install @tanstack/react-query zod

# Шаг 3: Следовать плану
# docs/03_REFACTORING_PLAN.md → Фаза 0 → Фаза 1 → ...

# Время: 5-7 дней
```

### Сценарий Б: "Срочно нужна новая фича"
```bash
# Шаг 1: Создать service файл
mkdir -p lib/services
touch lib/services/my-service.ts

# Шаг 2: Вынести логику туда
export async function myFeature(data: any) {
  const response = await fetch('/api/my-endpoint', {
    method: 'POST',
    body: JSON.stringify(data)
  })
  return response.json()
}

# Шаг 3: Использовать в компоненте
import { myFeature } from '@/lib/services/my-service'

const handleAction = async () => {
  try {
    await myFeature(data)
    toast.success('Готово!')
  } catch (error) {
    toast.error('Ошибка!')
  }
}

# Время: 1-2 часа
# ⚠️ Технический долг растёт!
```

---

## 📂 Где что находится

```
syndicateadmin/
├── app/
│   ├── api/                    # API endpoints
│   │   ├── materials/          # CRUD материалов
│   │   ├── local-feeds/        # CRUD фидов
│   │   ├── stats/              # Статистика
│   │   └── sync/               # Синхронизация
│   ├── page.tsx                # Главная страница
│   └── layout.tsx              # ⚠️ Здесь cron (проблема!)
│
├── components/
│   ├── feed-manager.tsx        # Управление фидами (400 строк!)
│   ├── header.tsx              # Хедер
│   └── ui/                     # shadcn/ui компоненты
│
├── lib/
│   ├── db.ts                   # Database service
│   ├── cron.ts                 # ⚠️ Cron job
│   ├── rss-parser.ts           # RSS парсер
│   ├── types.ts                # TypeScript типы
│   └── utils.ts                # Утилиты
│
└── docs/                       # 📚 Документация
    ├── 00_README.md            # ← НАЧНИ ОТСЮДА
    ├── 01_CODE_AUDIT.md
    ├── 02_BUSINESS_REQUIREMENTS.md
    ├── 03_REFACTORING_PLAN.md
    └── 04_ARCHITECTURE.md
```

---

## 🛠 Частые операции

### Добавить новый API endpoint
```typescript
// app/api/my-endpoint/route.ts
import { NextRequest, NextResponse } from 'next/server'

export async function POST(request: NextRequest) {
  try {
    const body = await request.json()
    
    // Твоя логика здесь
    
    return NextResponse.json({
      success: true,
      data: { /* результат */ }
    })
  } catch (error) {
    return NextResponse.json(
      { success: false, error: 'Error message' },
      { status: 500 }
    )
  }
}
```

### Добавить новый компонент с данными
```typescript
// БЕЗ рефакторинга (быстро, но плохо)
'use client'
import { useEffect, useState } from 'react'

export function MyComponent() {
  const [data, setData] = useState([])
  const [loading, setLoading] = useState(true)
  
  useEffect(() => {
    fetch('/api/my-endpoint')
      .then(r => r.json())
      .then(result => {
        setData(result.data)
        setLoading(false)
      })
  }, [])
  
  if (loading) return <div>Loading...</div>
  return <div>{/* UI */}</div>
}

// ПОСЛЕ рефакторинга (правильно)
'use client'
import { useMyData } from '@/hooks/useMyData'

export function MyComponent() {
  const { data, isLoading } = useMyData()
  
  if (isLoading) return <div>Loading...</div>
  return <div>{/* UI */}</div>
}
```

### Добавить валидацию Zod
```typescript
import { z } from 'zod'

const MySchema = z.object({
  name: z.string().min(1, 'Обязательное поле'),
  email: z.string().email('Некорректный email'),
  age: z.number().min(18, 'Минимум 18 лет'),
})

// В API route
try {
  const validatedData = MySchema.parse(body)
  // Используй validatedData (типизированные данные)
} catch (error) {
  if (error instanceof z.ZodError) {
    return NextResponse.json(
      { success: false, error: 'Validation error', details: error.errors },
      { status: 400 }
    )
  }
}
```

---

## 🔍 Отладка

### Проблема: API возвращает ошибку
```bash
# 1. Проверь логи в терминале
npm run dev

# 2. Проверь Network tab в DevTools
# F12 → Network → найди запрос → Response

# 3. Проверь server-side логи
# Смотри console.log в терминале, где запущен dev server
```

### Проблема: Cron не работает
```bash
# 1. Проверь, запускается ли при старте
# Ищи в логах: "Cron job initialized"

# 2. Проверь Railway логи
railway logs

# 3. Вызови синхронизацию вручную
# Нажми кнопку "Синхронизировать" в UI
# или
curl -X POST http://localhost:3000/api/sync
```

### Проблема: TypeScript ошибки
```bash
# 1. Проверь синтаксис
npm run build

# 2. Перезапусти TypeScript сервер в VS Code
# CMD+Shift+P → "TypeScript: Restart TS Server"

# 3. Удали кеш
rm -rf .next
npm run dev
```

---

## 📊 Метрики для мониторинга

```
┌─────────────────────────────────────────────────────┐
│ Что мониторить                                      │
├─────────────────────────────────────────────────────┤
│ ✅ Количество материалов в БД                       │
│ ✅ Количество фидов                                 │
│ ✅ Последняя синхронизация (не > 10 минут назад)    │
│ ✅ Ошибки в логах (должно быть 0)                   │
│ ⚠️ Время загрузки страниц (< 1 сек)                │
│ ⚠️ API response time (< 500ms)                      │
└─────────────────────────────────────────────────────┘
```

---

## 🚨 Безопасность

```
❌ КРИТИЧНО: Нет аутентификации!
   Любой может редактировать материалы.

✅ Решение: Добавить NextAuth
   npm install next-auth
   https://next-auth.js.org/getting-started/example

⏱ Время: 2-3 часа
🔥 Приоритет: P0 (сделать немедленно!)
```

---

## 💾 Backup БД (важно!)

```bash
# Создать backup
pg_dump $DATABASE_URL > backup_$(date +%Y%m%d).sql

# Восстановить из backup
psql $DATABASE_URL < backup_20251110.sql

# ⚠️ Делай backup ПЕРЕД рефакторингом!
```

---

## 🧪 Тестирование изменений

```bash
# 1. Тестировать локально
npm run dev
# → Открой http://localhost:3000
# → Проверь каждую функцию

# 2. Проверить build
npm run build
npm run start

# 3. Деплой на staging (Railway)
git push origin feature-branch
# → Создай staging окружение в Railway

# 4. Только после тестирования → production
git checkout main
git merge feature-branch
git push origin main
```

---

## 📞 Что делать, если застрял

### Вариант 1: Прочитать документацию
```
docs/
├── 00_README.md         ← Быстрое резюме
├── 01_CODE_AUDIT.md     ← Все проблемы с примерами
├── 03_REFACTORING_PLAN.md  ← Пошаговый план
└── 04_ARCHITECTURE.md   ← Диаграммы
```

### Вариант 2: Поискать похожую проблему
```bash
# Поиск в коде
grep -r "похожий код" .

# Поиск в GitHub Issues
# github.com/x35m/syndicateadmin/issues
```

### Вариант 3: Создать Issue
```markdown
# Заголовок
[Проблема] Краткое описание

# Описание
- Что пытаюсь сделать
- Что происходит
- Что ожидаю
- Код (если применимо)
- Логи ошибок
```

---

## ⚡ Quick Commands

```bash
# Разработка
npm run dev           # Запустить dev сервер
npm run build         # Собрать production build
npm run start         # Запустить production

# Линтинг
npm run lint          # Проверить код

# Git
git status            # Статус изменений
git diff              # Посмотреть изменения
git add .             # Добавить все файлы
git commit -m "msg"   # Закоммитить
git push              # Отправить на GitHub

# Railway
railway login         # Авторизоваться
railway logs          # Посмотреть логи
railway up            # Деплой
```

---

## 🎯 Checklist перед деплоем

- [ ] `npm run build` проходит без ошибок
- [ ] TypeScript без ошибок
- [ ] Все функции протестированы локально
- [ ] Создан backup БД
- [ ] .env переменные настроены
- [ ] Логи чистые (нет console.error)
- [ ] Railway cron настроен (если нужен)

---

## 🏆 Best Practices

### DO ✅
```typescript
// Используй типы
interface User {
  id: string
  name: string
}

// Обрабатывай ошибки
try {
  await fetchData()
} catch (error) {
  toast.error('Ошибка!')
  console.error(error)
}

// Используй toast для уведомлений
toast.success('Готово!')

// Валидируй входные данные
const result = schema.parse(data)
```

### DON'T ❌
```typescript
// Не используй any
const data: any = ... // ❌

// Не игнорируй ошибки
try {
  ...
} catch (error) {} // ❌

// Не используй alert
alert('Ошибка!') // ❌

// Не дублируй fetch логику
// Вынеси в service
```

---

## 📚 Полезные ссылки (сохрани!)

- **Документация проекта:** `docs/INDEX.md`
- **React Query:** https://tanstack.com/query/latest
- **Zod:** https://zod.dev
- **Next.js:** https://nextjs.org/docs
- **shadcn/ui:** https://ui.shadcn.com
- **Railway:** https://docs.railway.app

---

## 🔄 Статус проекта (обновляй регулярно)

```
Последнее обновление: 10 ноября 2025

┌──────────────────────────────────────────┐
│ Рефакторинг: ⬜⬜⬜⬜⬜⬜⬜ (0/7 фаз)     │
├──────────────────────────────────────────┤
│ ⬜ Фаза 0: Подготовка                    │
│ ⬜ Фаза 1: Архитектурные слои            │
│ ⬜ Фаза 2: Рефакторинг компонентов       │
│ ⬜ Фаза 3: Исправление Cron              │
│ ⬜ Фаза 4: Валидация                     │
│ ⬜ Фаза 5: Пагинация                     │
│ ⬜ Фаза 6: Тестирование                  │
│ ⬜ Фаза 7: Деплой                        │
└──────────────────────────────────────────┘

# После завершения фазы отмечай ✅
```

---

**Эта шпаргалка - твой главный инструмент при работе с проектом!**

*Добавь в закладки: `docs/CHEATSHEET.md`*
