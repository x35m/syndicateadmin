# Визуализация архитектуры Syndicate Admin

**Дата:** 10 ноября 2025

---

## Текущая архитектура (ПРОБЛЕМНАЯ)

```
┌────────────────────────────────────────────────────────────────┐
│                         ПРОБЛЕМЫ                               │
│  ❌ Всё в одном слое                                           │
│  ❌ Компоненты делают fetch                                    │
│  ❌ Дублирование логики                                        │
│  ❌ Нет типизации                                              │
└────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      Browser (Client Side)                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌───────────────────────────────────────────────────────┐   │
│   │  React Components (UI + Logic + Data)                 │   │
│   │                                                         │   │
│   │  app/page.tsx                                          │   │
│   │    ├─ useState, useEffect                              │   │
│   │    ├─ fetch('/api/stats')                              │   │
│   │    ├─ try/catch (плохая обработка)                     │   │
│   │    └─ alert() для ошибок ❌                            │   │
│   │                                                         │   │
│   │  components/feed-manager.tsx                           │   │
│   │    ├─ useState, useEffect                              │   │
│   │    ├─ fetch('/api/local-feeds') (дубль!)               │   │
│   │    ├─ try/catch (дубль!)                               │   │
│   │    └─ toast() для уведомлений                          │   │
│   │                                                         │   │
│   │  app/layout.tsx ❌ ПРОБЛЕМА                            │   │
│   │    └─ initCronJob() в useEffect                        │   │
│   └───────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              │ HTTP Requests (fetch)            │
│                              ▼                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                     Next.js Server (API Routes)                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   app/api/materials/route.ts                                   │
│     ├─ GET: getAllMaterials() ❌ нет пагинации                │
│     ├─ PATCH: updateStatus() ❌ нет валидации                 │
│     └─ DELETE: deleteMaterial()                                │
│                                                                 │
│   app/api/local-feeds/route.ts                                 │
│     ├─ GET: getAllFeeds()                                      │
│     ├─ POST: addFeed() ❌ нет валидации                       │
│     ├─ PATCH: updateFeed()                                     │
│     └─ DELETE: deleteFeed()                                    │
│                                                                 │
│   app/api/sync/route.ts                                        │
│     └─ POST: fetchAndSaveMaterials()                           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ SQL queries
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Database Layer (lib/db.ts)                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   DatabaseService                                               │
│     ├─ init() - создает таблицы                                │
│     ├─ getAllMaterials() ❌ без пагинации                      │
│     ├─ saveMaterials()                                         │
│     ├─ updateMaterialStatus()                                  │
│     ├─ getAllFeeds()                                           │
│     └─ addFeed()                                               │
│                                                                 │
│   ❌ Прямые SQL запросы (захардкожены)                         │
│   ❌ Нет миграций                                              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      PostgreSQL (Railway)                       │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    Background Jobs (lib/cron.ts)                │
│                     ❌ ЗАПУСКАЕТСЯ В REACT LAYOUT!              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   node-cron                                                     │
│     └─ schedule('*/5 * * * *')                                  │
│         └─ fetchAndSaveMaterials()                              │
│             └─ rssParser.parseFeed()                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Целевая архитектура (ПОСЛЕ РЕФАКТОРИНГА)

```
┌────────────────────────────────────────────────────────────────┐
│                        УЛУЧШЕНИЯ                               │
│  ✅ Чёткие слои                                                │
│  ✅ Hooks для логики                                           │
│  ✅ Services для API                                           │
│  ✅ Типизация везде                                            │
│  ✅ Централизованная обработка ошибок                          │
└────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      Browser (Client Side)                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │             Presentation Layer (UI)                     │  │
│  │                                                           │  │
│  │  app/page.tsx                                            │  │
│  │    ├─ const { stats } = useMaterials()                   │  │
│  │    └─ <StatsCards stats={stats} />                       │  │
│  │                                                           │  │
│  │  modules/feeds/components/FeedManager.tsx                │  │
│  │    ├─ const { feeds, addFeed, deleteFeed } = useFeeds()  │  │
│  │    └─ <FeedList feeds={feeds} />                         │  │
│  └─────────────────────────────────────────────────────────┘  │
│                              │                                  │
│                              │ Hooks API                        │
│                              ▼                                  │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │            Hooks Layer (State Management)                │  │
│  │                                                           │  │
│  │  modules/feeds/hooks/useFeeds.ts                         │  │
│  │    ├─ useQuery(['feeds'], feedsService.getAll)           │  │
│  │    ├─ useMutation(feedsService.add)                      │  │
│  │    └─ useMutation(feedsService.delete)                   │  │
│  │                                                           │  │
│  │  modules/materials/hooks/useMaterials.ts                 │  │
│  │    ├─ useQuery(['materials', status], ...)               │  │
│  │    └─ useMutation(materialsService.updateStatus)         │  │
│  └─────────────────────────────────────────────────────────┘  │
│                              │                                  │
│                              │ Service API                      │
│                              ▼                                  │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │            Services Layer (Business Logic)               │  │
│  │                                                           │  │
│  │  modules/feeds/services/feedsService.ts                  │  │
│  │    ├─ getAll(): Promise<Feed[]>                          │  │
│  │    ├─ add(input: AddFeedInput): Promise<Result>          │  │
│  │    ├─ update(input: UpdateFeedInput): Promise<void>      │  │
│  │    └─ delete(feedId: string): Promise<void>              │  │
│  │                                                           │  │
│  │  modules/materials/services/materialsService.ts          │  │
│  │    ├─ getAll(status?, page?, limit?): Promise<Result>    │  │
│  │    ├─ updateStatus(id, status): Promise<void>            │  │
│  │    └─ delete(id): Promise<void>                          │  │
│  └─────────────────────────────────────────────────────────┘  │
│                              │                                  │
│                              │ HTTP (fetch)                     │
│                              ▼                                  │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │         Validation Layer (Zod Schemas)                   │  │
│  │                                                           │  │
│  │  modules/feeds/schemas/index.ts                          │  │
│  │    ├─ AddFeedSchema.parse(data)                          │  │
│  │    └─ UpdateFeedSchema.parse(data)                       │  │
│  │                                                           │  │
│  │  modules/materials/schemas/index.ts                      │  │
│  │    └─ UpdateMaterialSchema.parse(data)                   │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │           Error Handling (Error Boundary)                │  │
│  │                                                           │  │
│  │  shared/components/ErrorBoundary.tsx                     │  │
│  │    └─ componentDidCatch() → Toast + Sentry               │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                     Next.js Server (API Routes)                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │              API Routes (Type-safe)                      │  │
│  │                                                           │  │
│  │  app/api/materials/route.ts                              │  │
│  │    ├─ GET: ✅ с пагинацией, валидацией                   │  │
│  │    ├─ PATCH: ✅ с Zod валидацией                         │  │
│  │    └─ DELETE: ✅ с проверкой прав                        │  │
│  │                                                           │  │
│  │  app/api/local-feeds/route.ts                            │  │
│  │    ├─ GET: ✅ типизированный ответ                       │  │
│  │    ├─ POST: ✅ AddFeedSchema.parse()                     │  │
│  │    └─ PATCH: ✅ UpdateFeedSchema.parse()                 │  │
│  │                                                           │  │
│  │  app/api/health/route.ts ✅ НОВЫЙ                        │  │
│  │    └─ GET: инициализация cron при первом запросе         │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ SQL queries (через ORM/Query Builder)
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                 Data Access Layer (Repository Pattern)          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  shared/lib/db/DatabaseService.ts                               │
│    ├─ getMaterialsPaginated(page, limit, status) ✅            │
│    ├─ saveMaterials(materials) ✅                              │
│    ├─ getAllFeeds() ✅                                         │
│    └─ addFeed(feed) ✅                                         │
│                                                                 │
│  ✅ Типизированные методы                                      │
│  ✅ Подготовлено для миграций                                  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      PostgreSQL (Railway)                       │
│  ✅ Индексы на месте                                           │
│  ✅ Оптимизированные запросы                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│            Background Jobs (Отдельный процесс) ✅               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Railway Cron Job / External Scheduler                          │
│    └─ Вызывает: POST /api/health каждые 5 минут                │
│                                                                 │
│  lib/init.ts                                                    │
│    ├─ initializeApp() ✅                                       │
│    │   ├─ db.init()                                            │
│    │   ├─ runInitialFetch()                                    │
│    │   └─ initCronJob()                                        │
│    └─ Singleton pattern (один раз)                             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Поток данных: Добавление фида

### ❌ СЕЙЧАС (Проблемный)

```
User clicks "Добавить фид"
    │
    ▼
FeedManager.tsx
    ├─ useState для URL
    ├─ handleAddFeed() {
    │    setAdding(true)
    │    try {
    │      const response = await fetch('/api/local-feeds', { ... })
    │      const result = await response.json()
    │      if (result.success) {
    │        toast.success(...)
    │        setNewFeedUrl('')
    │        await fetchFeeds() // ещё один fetch!
    │      } else {
    │        toast.error(...)
    │      }
    │    } catch (error) {
    │      toast.error('Ошибка')
    │    }
    │    setAdding(false)
    │  }
    └─ 50 строк кода в компоненте!

ПРОБЛЕМЫ:
- Бизнес-логика в UI
- Дублирование fetch логики
- Сложно тестировать
- Нет типизации
```

### ✅ ПОСЛЕ РЕФАКТОРИНГА (Правильный)

```
User clicks "Добавить фид"
    │
    ▼
FeedManager.tsx (5 строк)
    │
    ├─ const { addFeed, isAdding } = useFeeds()
    └─ <Button onClick={() => addFeed({ feedUrl })} disabled={isAdding} />
    │
    ▼
useFeeds() hook
    │
    ├─ useMutation({
    │    mutationFn: feedsService.add,
    │    onSuccess: () => {
    │      queryClient.invalidateQueries(['feeds'])
    │      toast.success('Фид добавлен!')
    │    }
    │  })
    │
    ▼
feedsService.add(input)
    │
    ├─ Валидация: AddFeedSchema.parse(input) ✅
    ├─ const response = await fetch('/api/local-feeds', { ... })
    ├─ const result: ApiResponse<Result> = await response.json()
    ├─ if (!result.success) throw new Error(...)
    └─ return result.data
    │
    ▼
API Route: POST /api/local-feeds
    │
    ├─ Валидация: AddFeedSchema.parse(body) ✅
    ├─ const feed = await db.addFeed(validatedData)
    ├─ const stats = await rssParser.import(feed)
    └─ return NextResponse.json({ success: true, data: { stats } })
    │
    ▼
DatabaseService.addFeed()
    │
    └─ INSERT INTO feeds ... RETURNING *
    │
    ▼
PostgreSQL

УЛУЧШЕНИЯ:
✅ UI - только отображение
✅ Логика в Services
✅ Типизация везде
✅ Легко тестировать
✅ Нет дублирования
```

---

## Структура папок

### ❌ СЕЙЧАС

```
syndicateadmin/
├── app/
│   ├── api/                    # API routes
│   │   ├── local-feeds/
│   │   ├── materials/
│   │   ├── stats/
│   │   └── sync/
│   ├── materials/
│   │   └── page.tsx
│   ├── globals.css
│   ├── layout.tsx             # ❌ Cron тут!
│   └── page.tsx
├── components/
│   ├── feed-manager.tsx       # 400 строк!
│   ├── header.tsx
│   └── ui/
├── lib/
│   ├── cron.ts
│   ├── db.ts
│   ├── rss-parser.ts
│   ├── types.ts
│   └── utils.ts
└── docs/

ПРОБЛЕМЫ:
- Всё вперемешку
- Нет модульности
- Сложно найти нужное
```

### ✅ ПОСЛЕ РЕФАКТОРИНГА

```
syndicateadmin/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── api/
│   │   │   ├── health/         # ✅ Инициализация
│   │   │   ├── materials/
│   │   │   └── local-feeds/
│   │   ├── materials/
│   │   ├── layout.tsx          # Только UI
│   │   └── page.tsx
│   │
│   ├── modules/                # ✅ Модульная структура
│   │   ├── feeds/
│   │   │   ├── api/            # API routes
│   │   │   ├── components/     # FeedManager, FeedList
│   │   │   ├── hooks/          # useFeeds, useFeedActions
│   │   │   ├── services/       # feedsService
│   │   │   ├── types/          # Feed, FeedStatus
│   │   │   └── schemas/        # Zod schemas
│   │   │
│   │   ├── materials/
│   │   │   ├── api/
│   │   │   ├── components/     # MaterialsList, StatsCards
│   │   │   ├── hooks/          # useMaterials
│   │   │   ├── services/       # materialsService
│   │   │   ├── types/
│   │   │   └── schemas/
│   │   │
│   │   └── sync/
│   │       ├── services/       # syncService
│   │       └── types/
│   │
│   └── shared/                 # ✅ Общий код
│       ├── components/
│       │   ├── ErrorBoundary.tsx
│       │   └── ui/
│       ├── hooks/
│       │   └── useDebounce.ts
│       ├── lib/
│       │   ├── api-client.ts   # HTTP client
│       │   ├── db/             # DatabaseService
│       │   ├── init.ts         # ✅ Инициализация
│       │   └── query-client.ts # React Query
│       ├── types/
│       │   └── api.ts          # ApiResponse, PaginatedResponse
│       └── utils/
│
├── docs/                       # ✅ Документация
│   ├── 00_README.md
│   ├── 01_CODE_AUDIT.md
│   ├── 02_BUSINESS_REQUIREMENTS.md
│   ├── 03_REFACTORING_PLAN.md
│   └── 04_ARCHITECTURE.md (этот файл)
│
└── package.json

УЛУЧШЕНИЯ:
✅ Чёткая модульность
✅ Легко найти нужное
✅ Масштабируемо
✅ Понятная структура
```

---

## Диаграмма зависимостей

### ❌ СЕЙЧАС (Тесная связанность)

```
┌──────────────┐        ┌──────────────┐
│ page.tsx     │───────►│ /api/stats   │
│              │        │              │
│ - useState   │        │ - db.query   │
│ - fetch()    │        └──────────────┘
│ - try/catch  │                │
└──────────────┘                │
       │                        │
       │                        ▼
       │               ┌──────────────┐
       │               │ DatabaseService
       │               │              │
       │               │ - SQL query  │
       │               └──────────────┘
       │                        │
       ▼                        │
┌──────────────┐                │
│feed-manager  │────────────────┘
│              │
│ - useState   │
│ - fetch()    │
│ - try/catch  │
└──────────────┘

ПРОБЛЕМЫ:
- Всё зависит от всего
- Нельзя переиспользовать
- Сложно тестировать
```

### ✅ ПОСЛЕ (Слабая связанность)

```
┌──────────────┐
│ page.tsx     │
│ (только UI)  │
└──────────────┘
       │
       │ использует
       ▼
┌──────────────┐
│ useMaterials()│
│ (hook)       │
└──────────────┘
       │
       │ использует
       ▼
┌──────────────┐
│materialsService│
│ (бизнес-логика)│
└──────────────┘
       │
       │ вызывает
       ▼
┌──────────────┐
│ API Route    │
│ /api/materials│
└──────────────┘
       │
       │ использует
       ▼
┌──────────────┐
│DatabaseService│
│ (data access)│
└──────────────┘
       │
       ▼
┌──────────────┐
│ PostgreSQL   │
└──────────────┘

УЛУЧШЕНИЯ:
✅ Чёткая иерархия
✅ Каждый слой независим
✅ Легко тестировать
✅ Легко заменить слой
```

---

## Обработка ошибок

### ❌ СЕЙЧАС

```
try {
  const response = await fetch(...)
  // ...
} catch (error) {
  console.error(error)        // Теряется
  alert('Ошибка!')            // Плохой UX
}
```

### ✅ ПОСЛЕ

```
┌─────────────────────────────────────────────┐
│         ErrorBoundary (компонент)           │
│  - Ловит React ошибки                       │
│  - Показывает fallback UI                   │
│  - Отправляет в Sentry                      │
└─────────────────────────────────────────────┘
                    │
                    │ оборачивает
                    ▼
┌─────────────────────────────────────────────┐
│            React Query                      │
│  - onError: toast.error()                   │
│  - Retry механизмы                          │
│  - Error state в UI                         │
└─────────────────────────────────────────────┘
                    │
                    │ использует
                    ▼
┌─────────────────────────────────────────────┐
│            Services                         │
│  - if (!result.success) throw new Error()   │
│  - Типизированные ошибки                    │
└─────────────────────────────────────────────┘
                    │
                    │ вызывает
                    ▼
┌─────────────────────────────────────────────┐
│            API Routes                       │
│  - Zod validation errors                    │
│  - HTTP status codes                        │
│  - Error logging                            │
└─────────────────────────────────────────────┘

РЕЗУЛЬТАТ:
✅ Ошибки не теряются
✅ Пользователь видит что произошло
✅ Логи для отладки
✅ Graceful degradation
```

---

## Сравнение: Добавить новую функцию

### ❌ СЕЙЧАС: "Хочу добавить теги для материалов"

**Проблемы:**
1. Нужно менять 5+ файлов
2. Дублировать fetch логику
3. Добавлять типы (или any)
4. Обрабатывать ошибки вручную
5. Тестировать всё руками

**Время:** 2-3 дня 😢

### ✅ ПОСЛЕ: "Хочу добавить теги для материалов"

**Шаги:**
```
1. Создать модуль /modules/tags/
   ├── types/index.ts         (15 мин)
   ├── schemas/index.ts       (15 мин)
   ├── services/tagsService.ts (30 мин)
   ├── hooks/useTags.ts       (30 мин)
   └── components/TagManager.tsx (1 час)

2. Добавить API route
   └── app/api/tags/route.ts  (30 мин)

3. Обновить DB
   └── lib/db.ts              (30 мин)

4. Интегрировать в UI
   └── materials/page.tsx     (30 мин)
```

**Время:** 4-5 часов ✅

**Почему быстрее?**
- ✅ Есть patterns
- ✅ Переиспользуется код
- ✅ Типы помогают
- ✅ Тестировать легче

---

## Заключение

### Ключевые принципы новой архитектуры

1. **Separation of Concerns** (Разделение ответственности)
   - UI только отображает
   - Hooks управляют состоянием
   - Services содержат логику
   - API routes - роутинг

2. **Single Responsibility** (Единая ответственность)
   - Каждый модуль делает одну вещь
   - Легко найти и изменить

3. **DRY (Don't Repeat Yourself)**
   - Код не дублируется
   - Общая логика в shared/

4. **Type Safety**
   - TypeScript везде
   - Никаких any
   - Compile-time проверки

5. **Error Handling**
   - Централизованная обработка
   - Graceful degradation
   - Логирование

---

**Следующий документ:** После завершения рефакторинга создать `05_TESTING_GUIDE.md`
